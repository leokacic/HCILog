import React from "react";
import { Layout } from "../components";
import MainNews from "../components/MainNews";
import data from '../../data.json';

export default () => (
  <Layout data={data.PUTOVANJA}>
  </Layout>
);
